# Solo para levantar local con `--spring.profiles.active=dev`: agrega el seed de un
# usuario demo (ver db/migration/dev/) que jamas debe correr contra produccion.
spring.flyway.locations=classpath:db/migration,classpath:db/migration/dev
